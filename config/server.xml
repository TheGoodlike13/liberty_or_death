<server>
    <featureManager>
        <feature>servlet-3.1</feature>
        <feature>appSecurity-2.0</feature>
    </featureManager>

    <kerberos configFile="goodlike.conf" keytab="goodlike.keytab"/>

    <application location="${whereMyWarAt}"/>
</server>
